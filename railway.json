{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "nixpacksPlanPath": "nixpacks.toml"
  },
  "deploy": {
    "startCommand": "./target/release/apt-server",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 10
  }
}
